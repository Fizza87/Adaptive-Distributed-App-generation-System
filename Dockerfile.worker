FROM node:18-alpine

# Set working directory
WORKDIR /app

# Copy worker script
COPY worker.js .

# Install dependencies that might be needed for code generation
RUN apk add --no-cache bash curl

# Create output directory
RUN mkdir -p output

# Set environment
ENV NODE_ENV=production

# Keep container running
CMD ["tail", "-f", "/dev/null"]